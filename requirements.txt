# Core dependencies
opencv-python>=4.5.0
mediapipe>=0.8.9
numpy>=1.19.0
pillow>=8.0.0
pandas>=1.3.0
matplotlib>=3.4.0
scikit-learn>=0.24.0
fer>=22.4.0

# Optional but recommended
dlib>=19.22.0  # For facial landmark detection (requires CMake and C++ build tools)
imutils>=0.5.4  # For image processing utilities

# Development dependencies
pytest>=6.2.0  # For testing
black>=21.5b2  # For code formatting
flake8>=3.9.0  # For linting

# Notes:
# - tkinter is included with Python 3.x standard library (no pip install needed)
# - dlib may require: sudo apt-get install cmake build-essential (Linux)
#                     or Xcode command line tools (macOS)
# - Python 3.7+ required